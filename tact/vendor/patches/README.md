# Vendor Patches

This directory contains patches and compatibility shims for the vendored pyprima code.

## Application

These patches are automatically applied by the `update_vendor.sh` script after updating the vendored code from the scipy repository.
